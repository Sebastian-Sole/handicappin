# Frozen rubric — `forgot-password`.
# Native: apps/native/app/(auth)/forgot-password.tsx (testID "forgot-password-screen").
# Web reference: /forgot-password, phone viewport. Web chrome not a finding.
screen: forgot-password
rubric_version: bring-up-v1
items:
  layout_structure:
    desc: >
      Narrow column: "Reset Password" heading, two-line muted description,
      Email labelled field, full-width primary "Send verification code"
      button. Nothing else.
  color_tokens:
    desc: >
      Background/foreground/muted as the auth cluster; primary CTA green
      with primary-foreground label; bordered input.
    pairings_used:
      - body-on-page
      - muted-on-page
      - primary-foreground-on-primary
  type_hierarchy:
    desc: >
      H1 extra-bold; body muted description; small label; label-size button
      text. All Inter.
  components_present:
    desc: >
      One bordered input; one full-width primary button labelled "Send
      verification code".
a11y:
  headings: [Reset Password]
  markers: [forgot-password-screen, forgot-password-email, forgot-password-submit]
