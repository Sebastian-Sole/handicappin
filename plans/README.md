# Implementation Plans

Generated by the improve skill (`/improve next` — direction audit) on 2026-07-02, at commit `469a53f`. Execute in the order below unless dependencies say otherwise. Each executor: read the plan fully before starting, honor its STOP conditions, and update your row when done.

Selection note: this run executed non-interactively, so plans were written for the top 5 direction findings by leverage (the skill's default). The full findings table, including items deliberately not planned, is below — the operator can request plans for any of them.

## Execution order & status

| Plan | Title | Priority | Effort | Depends on | Status |
|------|-------|----------|--------|------------|--------|
| 001 | Course-ingestion last mile: loader + provenance | P1 | M | — | DONE (approved; on `advisor/001-course-ingestion-loader`, unmerged) |
| 002 | Thin admin moderation console | P1 | M | — | DONE (approved; on `advisor/002-admin-moderation-console`, unmerged) |
| 003 | Rejection loop: reason, history, path forward | P2 | L | 002 | DONE (approved; on `advisor/003-rejection-loop`, which contains 001+002; unmerged) |
| 004 | Auth rate limiting before App Store launch | P1 | S–M | — | DONE (approved after 1 revision; on `advisor/004-auth-rate-limiting`, unmerged) |
| 005 | Billing lifecycle emails at the webhook chokepoint | P2 | M | — | DONE (approved after 1 revision; on `advisor/005-billing-lifecycle-emails`, unmerged) |

Status values: TODO | IN PROGRESS | DONE | BLOCKED (with one-line reason) | REJECTED (with one-line rationale)

## Dependency notes

- **003 requires 002**: the rejection reason is entered through the admin console 002 builds, and 003 modifies 002's `rejectSubmission` mutation and queue UI.
- 001, 002, 004, 005 are independent of each other and can run in any order or in parallel (separate worktrees recommended — 002 and 003 touch overlapping files if run together).
- 001 and 002 compose: ingested courses loaded as `pending` by 001's loader can be reviewed in 002's queue, but neither blocks the other (001 has an `--approve` flag for reviewed batches).
- 005 and open issue #134 touch the same webhook handler files — whichever lands second rebases around the other; see 005's maintenance notes.

## Direction findings NOT planned this run (candidates for a next run)

- **Course directory + SEO landing pages** (D6): `searchCourses` (typeahead, limit 10, `apps/web/server/api/routers/course.ts:41-68`) is the only consumer of the entire course/tee/hole dataset; a public `/courses` browse + per-course pages is the top-of-funnel surface the ingestion effort exists to feed. Deferred because it depends on plan 001 landing (and courses being approved) to have anything to show. Effort L.
- **Round-less course/tee submissions**: `submissions.roundId` is nullable by design (`apps/web/db/schema.ts:384`) but all four insert sites bind a fresh round (`round.ts:829-901`). Builds directly on 003's retained-lifecycle work; plan it after 003 lands. Effort M.
- **Native AI-scorecard camera flow (+ batch historical import spike)**: the extraction endpoint (`apps/web/app/api/ai/extract-scorecard/route.ts`) is web-only; native `rounds/add` defers it with logged rationale (`apps/native/app/rounds/add.tsx:1-11`). The camera-first platform is where this feature matters most. Effort M–L; tracked in BACKLOG §D.
- **Native advanced/educational calculators port**: native's own header documents the deferral (`apps/native/app/(tabs)/calculators.tsx:1-8`); the math is already shared in `@handicappin/handicap-core`, so these are UI-only ports. Effort M; tracked in BACKLOG §D.

## Decision drift to resolve (decide, don't build — one conversation each)

- **`.claude/plans/migrate-handicap-cron-to-vercel/`** (Dec 2025) says pg_cron+Vault is "not sustainable"; migrations from May 2026 (`20260502094814`, `20260505095530`) hardened pg_cron instead, and `apps/web/vercel.json` has only the reconcile-stripe cron. Either close the plan as superseded or execute it — the repo currently says both.
- **`.claude/plans/usga-round-verification.md`**: peer-attestation round verification was never built (zero schema/code hits); the admin approval workflow shipped instead. Decide whether peer attestation is still wanted; if not, close the plan.
- **`docs/future-features.md`** staleness: "Manual Reconciliation Trigger" and "Reconciliation Dashboard" are partially obsolete (reconcile ships as a Vercel cron already); "Subscription Change Notifications" is marked unbuilt but is partially wired (in-app path — plan 005 completes it). Worth a pruning pass.

## Findings considered and rejected

- **Geo/coordinate capture in the ingestion pipeline** (was part of plan 001): rejected by the owner on 2026-07-03 — no lat/lon columns, no scraper changes. If map/near-me features are ever wanted, coordinates require a re-scrape + backfill (recorded in 001's maintenance notes).
- **GDPR data export** ("missing"): already shipped — `auth.exportUserData` (`apps/web/server/api/routers/auth.ts:139`) + `data-export-section.tsx`. Not a gap.
- **Handicap-history trend view** ("missing sibling stat"): already shipped — `apps/web/components/charts/handicap-trend-chart.tsx`. Not a gap.
- **JWT in `monitor-queue.sh:97`**: it is the standard public `supabase-demo` local-dev token, not a real credential. No rotation needed; no finding.
- **#134 (Stripe optimistic lock)**: real and money-path, but already tracked as a GitHub issue with the Apple-side implementation as its template — re-planning it here would duplicate the tracker. Recommend doing it before launch.
- **Playwright E2E scaffolding**: absent but already specified file-by-file in `BACKLOG.md` (§"Playwright E2E Scaffolding") — a plan would restate the backlog entry. Execute from there when prioritized.
- **Native Google OAuth**: single stubbed component (`apps/native/components/auth/google-sign-in-button.tsx`), but gated on owner-side provisioning (iOS OAuth client + Supabase provider config), same bucket as the billing console setup. Code delta is small once provisioned.
- **future-features.md generic wishlist items** (notification center, Redis caching, audit logging, usage-based billing, fraud detection, metrics dashboards): their own trigger conditions (500–1000+ users, compliance requirements) don't hold yet — the doc's framework already defers them; the audit agrees.
